package objectOPs;

public class Static {
public static void main(String[] args) {
	/*STATIC VARIABLE:--
	 *static variable belongs to class not to object it is the class variable
	 * to access static variable we use classname.staticVariableName
	 * static variable could be final does not change or could be changeable
	 * NEVER NEVER use static variable with the object
	 * STATIC METHOD:--
	 * to access static variable we use classname.staticMethodName()
	 * NEVER NEVER use static method with the object
	 * static method  can only access static class variable
	 * static method  can only invoke static class method(method in method)
	 * 
	 * 
	 * 
	 * 
	 */
	
}
}
class emp{
	
}